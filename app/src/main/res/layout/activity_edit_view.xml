<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAF4C0"
    tools:context=".view.EditViewActivity">

    <EditText
        android:id="@+id/edit_title_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="top"
        android:text="제목"
        android:textStyle="bold"
        android:textSize="20dp"
        android:layout_marginTop="40dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        app:layout_constraintBottom_toTopOf="@+id/edit_content_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/edit_content_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="80dp"
        android:gravity="top"
        android:textSize="15dp"
        app:layout_constraintEnd_toEndOf="@+id/edit_title_view"
        app:layout_constraintStart_toStartOf="@+id/edit_title_view"
        app:layout_constraintTop_toBottomOf="@+id/edit_title_view"
        app:layout_constraintBottom_toBottomOf="parent">
    </EditText>

    <ListView
        android:id="@+id/imageListView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/edit_content_view">
    </ListView>

</androidx.constraintlayout.widget.ConstraintLayout>